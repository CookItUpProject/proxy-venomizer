- name: Name
  steps:
    - type: http
      method: GET
      url: https://example.com
      body: '{"key": "test"}'
      assertions:
        - result.statuscode ShouldEqual 200
        - result.bodyjson.status.code ShouldEqual 200
        - result.bodyjson.status.message ShouldEqual Ok
      expect:
        - status: 200